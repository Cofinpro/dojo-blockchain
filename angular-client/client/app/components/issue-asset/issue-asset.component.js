import template from './issue-asset.html';
import controller from './issue-asset.controller';
import './issue-asset.styl';

let issueAssetComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default issueAssetComponent;
