import template from './mitarbeiter.html';
import controller from './mitarbeiter.controller';
import './mitarbeiter.styl';

let mitarbeiterComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default mitarbeiterComponent;
